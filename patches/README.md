# Local patches

No PiForge compatibility patches are applied. Prefer upstream-supported
configuration and the pinned upstream recipes.

Any future patch must have an adjacent Markdown explanation with the defect,
affected upstream revision and issue URL, reproduction evidence, reason a
configuration change is insufficient, validation, and a removal condition.
Changing a source lock is not a compatibility patch.
